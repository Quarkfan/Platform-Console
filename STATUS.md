# Platform Console Status

The Dashboard now includes a permanent searchable usage-manual page in the lower-left auxiliary area and complete CRUD paths for providers, model deployments, routing policies, Bots, channels, context sources/bindings, capability bindings and schedules. Destructive actions use confirmations and dependency protection.

Version `0.1.0` is deployed. Better Auth accounts, roles, same-origin protected BFF, forced initial password change, all center management pages, local Bot chat, schedules, logs, diagnostics, Browser UI and read-only system assistant are implemented. Complex forms keep common fields concise and expose advanced settings for provider headers/weights, model capabilities/pricing, channel adapter JSON, context scope, Bot safety modes, schedule retries and capability-binding triggers. Overview probes liveness, readiness and version independently; diagnostics collect only bounded, redacted operational metadata. Fifteen tests pass, and all fifteen pages pass desktop/mobile layout acceptance without overflow or clipped controls.
