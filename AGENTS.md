# Platform Console collaboration

Independent Web Console and deployment repository. The browser calls only this BFF. It owns user accounts, sessions, RBAC, CSRF/origin protection, UI and deployment composition, but no center business facts. Dean makes final decisions; Codex must proactively recommend improvements.
